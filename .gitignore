
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
.venv/
venv/
ENV/
env/

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Project Specific
players.json
config.json
# config.json might contain sensitive keys or user specific settings, but for this tool it's just config.
# Ideally we keep a config.example.json but here we might want to track config.json or not.
# Given it stores local paths (like assets_path), maybe better to ignore or track template.
# But for simplicity let's track it unless it has API keys. It doesn't.
